# Autonomous-Object-Detection-Pipeline
A real-time, CPU-optimized object detection system using YOLOv8. It processes input images, detects multiple objects, and generates annotated outputs with bounding boxes, class labels, and confidence scores. The lightweight YOLOv8n model enables efficient detection on CPU-based systems.
